<?php

/*
|--------------------------------------------------------------------------
| Мониторинг недоставленных сообщений WhatsApp
|--------------------------------------------------------------------------
|
| Планировщик раз в пять минут пересчитывает исходящие сообщения журнала
| за скользящее окно. Тревога уходит админам в панель (database
| notifications) в двух случаях: доля failed среди исходящих превысила
| порог при достаточной выборке (всплеск), либо в причине отказа найден
| аккаунт-уровневый код Meta — тогда достаточно и одного сообщения,
| потому что такой код значит «не уходит ничего никому».
|
| min_failed защищает от ложных тревог на малой выборке: ночью бот шлёт
| единичные сообщения, и один случайный отказ — это 100% доли. cooldown
| гасит повторы: пока инцидент не устранён, каждый прогон находил бы те
| же отказы и слал бы то же уведомление заново.
*/

return [

    // Ширина скользящего окна, минуты.
    'window_minutes' => 30,

    // Минимальное число failed в окне, чтобы доля вообще рассматривалась.
    'min_failed' => 3,

    // Порог доли failed среди исходящих сообщений окна (0.25 = 25%).
    'failed_share' => 0.25,

    // Пауза между повторами одной и той же тревоги, минуты.
    'cooldown_minutes' => 60,

];

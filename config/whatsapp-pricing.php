<?php

/*
|--------------------------------------------------------------------------
| Тарифы сообщений WhatsApp (USD за одно доставленное сообщение)
|--------------------------------------------------------------------------
|
| Meta списывает за каждое доставленное сообщение по его категории: шаблоны —
| по категории шаблона (marketing / utility / authentication), нешаблонные
| ответы бота в 24-часовом окне — по категории service. Снимок тарифа
| сохраняется в channel_messages на момент отправки, по нему считается
| estimated_cost_usd. Это ОЦЕНКА по прайс-листу Meta, а не биллинг: наценка
| Dereu и налоги сюда не входят, точное списание сверяйте с Billing Hub.
| null = тариф неизвестен: такое сообщение получает cost_status=unknown
| (никогда не «ноль»).
|
| Сообщения оператора из приложения WhatsApp Business (коэкзистенс) Meta не
| тарифицирует — они здесь не участвуют и бесплатный лимит не расходуют.
| Бесплатное окно 72 часа после рекламы Click-to-WhatsApp не учитывается:
| такого трафика у проекта нет.
|
| Ставки — для Казахстана, основного рынка проекта: получатели за его
| пределами редки, их сообщения считаются по той же ставке.
*/

return [

    // Часовой пояс WhatsApp-аккаунта (WABA) в Meta Business Settings: в его
    // полночь вступает в силу новый прайс-лист и начинается месяц
    // бесплатного лимита сервисных сообщений.
    'timezone' => 'Asia/Almaty',

    // Прайс-листы по дате вступления в силу (в часовом поясе выше). Для
    // сообщения берётся последний лист, вступивший в силу к моменту отправки;
    // новый прайс добавляется новым ключом, старые остаются для истории.
    //
    // service_free_tier — сколько доставленных сервисных сообщений в
    // календарный месяц на номер бизнеса бесплатны (без переноса остатка);
    // null — лимита нет, все сервисные по ставке service.
    'rate_cards' => [

        // Казахстан в регионе «Other» (в «Rest of Central & Eastern Europe»
        // его нет). Нешаблонные сообщения в открытом окне бесплатны.
        '2026-07-01' => [
            'categories' => [
                'marketing' => 0.0604,
                'utility' => 0.0077,
                'authentication' => 0.0077,
                'service' => 0.0,
            ],
            'service_free_tier' => null,
        ],

        // Казахстан — отдельной строкой прайса. Сервисные сообщения платные
        // сверх 1 000 в месяц по ставке utility; utility-шаблоны в открытом
        // окне тоже платные.
        '2026-10-01' => [
            'categories' => [
                'marketing' => 0.0604,
                'utility' => 0.018,
                'authentication' => 0.018,
                'service' => 0.018,
            ],
            'service_free_tier' => 1000,
        ],

    ],

];
